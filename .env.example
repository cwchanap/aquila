# CockroachDB Database Configuration
# Connection string for CockroachDB (PostgreSQL-compatible)

# Production database URL (required, use your CockroachDB or PostgreSQL connection string)
DATABASE_URL=postgresql://username:password@host:26257/database?sslmode=verify-full

# Local development (CockroachDB single-node)
# Start local CockroachDB: cockroach start-single-node --insecure --listen-addr=localhost:26257
# DATABASE_URL=postgresql://root@localhost:26257/defaultdb?sslmode=disable

# Connection pool settings (optional, configured in code)
# - max: 10 connections
# - idleTimeoutMillis: 20000
# - connectionTimeoutMillis: 10000
